require 'spec_helper'

describe Ruboty::Attend do
  it 'has a version number' do
    expect(Ruboty::Attend::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
